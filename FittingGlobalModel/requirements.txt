# Python dependencies for the model-fitting and prediction notebooks.
# Install with:  pip install -r requirements.txt
#
# Versions are left unpinned. For an exactly reproducible environment,
# replace each line with the version you used, e.g. pyabc==0.12.13
# (run `pip freeze` in your working environment to get them).

numpy
pandas
scipy
matplotlib

# Approximate Bayesian computation used for every fit (Appendix B Section B4.3)
pyabc

# JIT compilation of the landscape functions in landscape_*.py
numba

# make_blobs, used to draw the initial cell positions
scikit-learn

# Histograms and KDE plots in fitting_plots.py and the notebooks
seaborn

# To run the notebooks
jupyterlab
