# Python dependencies for the analysis notebooks.
# Install with:  pip install -r requirements.txt
#
# Versions are left unpinned. For an exactly reproducible environment,
# replace each line with the version you used, e.g. scanpy==1.10.1
# (run `pip freeze` in your working environment to get them).

scanpy
anndata
numpy
pandas
scipy
matplotlib

# Required by sc.tl.leiden(flavor="igraph") in the clustering notebooks
leidenalg
igraph

# Required by the transition analysis (LDA, KDE plots)
scikit-learn
seaborn
